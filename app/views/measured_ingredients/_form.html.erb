<%= form_with(model: measured_ingredient) do |form| %>
  <% if measured_ingredient.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(measured_ingredient.errors.count, "error") %> prohibited this measured_ingredient from being saved:</h2>

      <ul>
        <% measured_ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :dish_id, style: "display: block" %>
    <%= form.text_field :dish_id %>
  </div>

  <div>
    <%= form.label :ingredient_id, style: "display: block" %>
    <%= form.text_field :ingredient_id %>
  </div>

  <div>
    <%= form.label :measurement_id, style: "display: block" %>
    <%= form.text_field :measurement_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
